{
  "name": "dns-leak-test",
  "version": "1.0.0",
  "description": "Responds to HTTP Requests with current DNS.",
  "main": "dns.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "DNS"
  ],
  "author": "Brandon Voth",
  "license": "ISC",
  "dependencies": {
    "axios": "^0.27.2",
    "express": "^4.18.1"
  }
}
